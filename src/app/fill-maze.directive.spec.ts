import { FillMazeDirective } from './fill-maze.directive';

describe('FillMazeDirective', () => {
  it('should create an instance', () => {
    const directive = new FillMazeDirective();
    expect(directive).toBeTruthy();
  });
});
