import { CreateWallWeightDirective } from './create-wall-weight.directive';

describe('CreateWallWeightDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateWallWeightDirective();
    expect(directive).toBeTruthy();
  });
});
