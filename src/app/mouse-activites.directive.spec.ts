import { MouseActivitesDirective } from './mouse-activites.directive';

describe('MouseActivitesDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseActivitesDirective();
    expect(directive).toBeTruthy();
  });
});
